name: autoscaling-demo-azure
description: Autoscaling demo with Azure
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: azure-python
